Git Version Control
    
    v1
    v2
    v3
new v4


>> It is distributed (on cloud)
>> Collaboration
>> Tracking Changes
>> Recovery

  https://git-scm.com/downloads

  Github
  BitBucket
  GitLabs
  GitPad

  >> For 1st on Github we need to configure github in our system
    Commands:
       >> git config --global user.name "Choudhary6109"
       >> git config --global user.gmail "mechoudhary102@gmail.com"
       

    Optional Command:  
          >> git config --global color.ui Auto  // this command suggest code and clour for error


   Mandatory Commands:
        >> git init  
        >> git remote add origin https://github.com/Choudhary6109/Majeed

        >> git pull origin main 
        >> git status    
        >> git commit -m "commit message"  
        >> git push


    Create a new Branch:
         >> git checkout -b "BranchName"


    Switch the Branch
        >> git checkout "branch name which need to be opened"


        git clone https://github.com/choudhary6109/Majeed.git